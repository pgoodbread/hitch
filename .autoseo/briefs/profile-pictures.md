# Brief: profile pictures

- Target keyword: `profile pictures`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational (with a commercial lean — readers are actively choosing which photos to upload)
- Funnel position: mid
- Volume: 27000/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Angle (unattended destination + framing note for the reviewer)

Only one destination is configured (`src/content/blog`), so no choice was needed. The keyword itself is generic — it also catches Facebook/LinkedIn avatar intent this site cannot serve. Write it squarely as **dating-app profile pictures**: which photos to lead with, how many, what order, what kills a profile. That serves the slice of the query that matches the site and the product, and it keeps the page from competing on generic-avatar intent it would lose anyway.

## Competitor coverage

- https://www.vidaselect.com/profile-pic-for-women

## Internal-link candidates (up to 6)

- /blog/why-your-first-tinder-photo-decides-everything — the natural deep-dive for the "lead photo" section; this guide should hand off to it rather than repeat it
- /blog/best-tinder-bios-for-guys — photos get the look, the bio closes it; link where the guide explains that pictures alone don't finish the job
- /blog/best-rizz-lines — the next step after the photos earn a match; link from the closing/what-happens-next section
- /blog/dating-me-is-like — bio-prompt companion for readers whose photos are fine but whose profile still reads flat
- /blog/swipe-left-meaning — useful context when explaining what makes someone swipe away from a photo set
- /blog/ask-girl-out-tinder — end-of-funnel handoff once the profile is doing its job

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `profile pictures` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `profile pictures` and stay ≤ 155 characters.
- Use `profile pictures` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
