# Brief: swipe left meaning

- Target keyword: `swipe left meaning`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: top
- Volume: 4100/mo · KD: 3
- Target length: 1200–1800 words
- Post type: guide

## Why this angle

Most pages ranking for this are one-paragraph glossary entries. Beat them by answering
the literal question fast (definition in the first 100 words), then going where the
glossary pages don't: _why people swipe left on you_, and what to change. That turns a
top-funnel definition query into a profile-audit read for a Tinder profile optimizer.

## Competitor coverage

- https://roast.dating/Blog

## Internal-link candidates (up to 6)

- /blog/why-your-first-tinder-photo-decides-everything — the first photo is the single biggest swipe-left trigger; the natural next click after "why do people swipe left on me"
- /blog/best-tinder-bios-for-guys — a weak or empty bio is the second-most-common reason a profile gets swiped left
- /blog/dating-me-is-like — bio prompt answers that dodge the clichés that trigger reflex left-swipes
- /blog/best-rizz-lines — what to do once a swipe right becomes a match (openers)

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `swipe left meaning` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `swipe left meaning` and stay ≤ 155 characters.
- Use `swipe left meaning` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.

## Coverage checklist

- Plain definition: swiping left = pass/reject; where the gesture comes from (Tinder, now generic slang).
- The mirror term: swipe right, and the "swipe left on X" figure of speech used outside dating apps.
- What happens after you swipe left — is it permanent, can you undo it (Rewind / Tinder+), do they get told.
- Why _you_ get swiped left on: first photo, group shots, no bio, mirror selfies, cliché prompts — each with the fix.
- Keep it non-judgmental and practical; the reader arrived for a definition, not a lecture.
