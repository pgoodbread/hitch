# Brief: tinder profile template

- Target keyword: `tinder profile template`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 1100/mo · KD: 6
- Target length: 1200–1800 words
- Post type: guide

## Why this one (unattended pick)

The scored top-20 was almost entirely rival dating-app brand queries (`secret benefits`,
`doublelist` misspellings, `seeking`) — unwinnable and non-converting. Ranked the full
cache by fit instead. This keyword is the closest match in the pool to what the product
actually does: rewrite your Tinder profile. It also balances the blog, which currently
leans messaging-heavy (rizz, pick-up lines, texting) against only three profile-side pages.

## Competitor coverage

- https://roast.dating/blog/tinder-profile-template

## Internal-link candidates (up to 6)

- /blog/best-tinder-bios-for-guys — the template's bio slot needs real examples; this is the deep-dive behind that section
- /blog/profile-pictures — the photo slots are half the template; link when specifying what goes in each slot
- /blog/why-your-first-tinder-photo-decides-everything — justifies the "slot 1" rule the template opens with
- /blog/ask-girl-out-tinder — the natural next step once the template starts producing matches
- /blog/swipe-left-meaning — basics context for readers new to Tinder mechanics who hit this page cold

## Structure notes

- Give an actual copy-pasteable template, not just advice about templates — fill-in-the-blank
  bio skeletons and a slot-by-slot photo order. That is the thing the SERP is not serving well.
- Include at least two or three worked examples of the template filled in for different guys
  (e.g. the outdoorsy one, the career-focused one, the funny one) so the page is usable, not abstract.
- Cover the prompts/interests fields, not only bio + photos.

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tinder profile template` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `tinder profile template` and stay ≤ 155 characters.
- Use `tinder profile template` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
