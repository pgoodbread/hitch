# Brief: what to talk about with a girl

- Target keyword: `what to talk about with a girl`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 2800/mo · KD: 4
- Target length: 1200–1800 words
- Post type: guide

## Why this topic

The site already covers the opener (`/blog/tinder-openers`, `/blog/slide-into-dm`) and
the close (`/blog/ask-girl-out-tinder`). This keyword sits in the gap between them: he
matched, he sent something, and now the conversation has to go somewhere. Write for the
guy who runs out of things to say by message four.

## Competitor coverage

- https://textgod.com/things-topics-to-talk-to-a-girl-about-over-text/

## Internal-link candidates (up to 6)

- /blog/tinder-openers — the previous step in the sequence; link when the article
  mentions how the conversation started
- /blog/ask-girl-out-tinder — the next step; the natural close once a topic lands
- /blog/meaning-in-text-from-a-girl — for reading whether a topic is actually landing
  ("..." and other low-effort replies)
- /blog/question-to-ask-your-crush — concrete questions that pair with the topic list
- /blog/how-to-hookup-with-a-girl — where the messaging leads if that's the goal
- /blog/pick-up-line — contrast: lines versus real conversation topics

## Angle notes

- Lead with the actual answer (topics that work + why), not a preamble about how hard
  texting is.
- Favour categories of topic with examples over a numbered list of 50 questions — the
  competitor already does the list, and thin lists don't hold a reader.
- Cover what to avoid (interview-mode questions, work small talk, anything requiring a
  one-word answer) — it's the part the competing page handles weakly.
- Keep it usable on Tinder specifically: short messages, she has other matches, the
  topic has to survive a 30-second reply window.

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `what to talk about with a girl` (exact, or its natural
  singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and
  add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title>
  tag and keeps `title` as the on-page H1.
- `description` must contain `what to talk about with a girl` and stay ≤ 155 characters.
- Use `what to talk about with a girl` in the first 100 words and in at least one `##`
  heading.
- Never write a `# ` heading in the body — the page would get two H1s.
