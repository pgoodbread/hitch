# Brief: negging

- Target keyword: `negging`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: top
- Volume: 32000/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Why this angle

This is a definition-first query. The site already runs a working explainer pattern
("Rizz Meaning, Explained", "Swipe Left Meaning on Tinder, Explained") — match it.
Reader wants: what negging is, where it came from, examples, why it does not work,
and what to do instead. The "what to do instead" section is the bridge to the site's
actual product (a profile and opener that work without manipulation), so land it
honestly rather than as a hard pitch.

Take a clear position: negging is a manipulation tactic, it reads as an insult, and
it costs matches. Do not present it neutrally as a "technique that sometimes works."
Cover the receiving end too — a lot of this traffic is people who were negged and
are naming the behaviour.

## Competitor coverage

- https://textgod.com/negging/

## Internal-link candidates (up to 6)

- /blog/rizz — the sibling explainer; "rizz" is the modern positive-charisma term negging pretends to be
- /blog/best-rizz-lines — the "what to do instead" payoff: openers that land without an insult
- /blog/ask-girl-out-tinder — natural next step once a conversation is actually going well
- /blog/best-tinder-bios-for-guys — attraction comes from the profile, not from a cheap line
- /blog/swipe-left-meaning — same explainer format, useful for the dating-vocabulary cluster

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `negging` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- Suggested `title`: `Negging: What It Is and Why It Backfires` (40 chars, keyword first).
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `negging` and stay ≤ 155 characters.
- Use `negging` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
