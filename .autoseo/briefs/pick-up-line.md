# Brief: pick up line

- Target keyword: `pick up line`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: top
- Volume: 8200/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Competitor coverage

- https://textgod.com/funny-tinder-pick-up-lines/

## Internal-link candidates (up to 6)

- /blog/best-rizz-lines — closest sibling: the rizz-line angle on the same opener problem; link when contrasting a canned line with a personalised one
- /blog/ask-girl-out-tinder — the natural next step after a pick up line lands; link from the "what to do after she replies" section
- /blog/rizz — defines the slang readers arriving from "rizz" adjacent queries expect; link on first use of "rizz"
- /blog/best-tinder-bios-for-guys — a pick up line only works if the profile behind it holds up; link when explaining why lines fail on weak profiles
- /blog/profile-pictures — same point from the photo side; link where the article argues the opener is not the bottleneck
- /blog/swipe-left-meaning — context for why most openers never get seen; link in the intro framing of match-to-reply drop-off

## Angle notes

- The head term is broad and mostly entertainment-driven. Win it by being the page that both delivers the lines readers came for AND explains the mechanic: why generic lines underperform, what makes a line land, how to adapt one to a specific profile.
- Cover the obvious sub-intents readers scan for: funny, smooth/flirty, cheesy, and profile-specific openers. Give real examples, not placeholders.
- Do not rewrite /blog/best-rizz-lines. That page is the "rizz lines" angle; this one is the broader pick up line guide and should link to it rather than repeat its list.
- Include a short "lines that get you left on read" counterpoint — negging, copy-pasted spam, anything sexual in the opener — and link out where relevant.

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `pick up line` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- Suggested: `Pick Up Lines That Actually Get Replies` (39 chars).
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `pick up line` and stay ≤ 155 characters.
- Use `pick up line` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
