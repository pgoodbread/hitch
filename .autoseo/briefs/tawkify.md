# Brief: tawkify

- Target keyword: `tawkify`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: commercial
- Funnel position: mid
- Volume: 19000/mo · KD: 3
- Target length: 1200–1800 words
- Post type: guide

## Why this topic

`tawkify` is a navigational-leaning brand query, but the people typing it are
evaluating a several-thousand-dollar matchmaking service. That is the same
audience as hitch's buyer — someone who has decided their dating results are a
problem worth paying to fix — just at the top of the price range. The angle is
an honest third-party review that helps them decide, and points out that the
profile itself is the cheapest variable to fix first. Do not write a puff piece
and do not write a hit piece; the searcher wants a straight answer on cost and
whether it works.

## Competitor coverage

- https://www.vidaselect.com/tawkify-cost

## Internal-link candidates (up to 6)

- /blog/is-tinder-gold-worth-it — the site's other "is paid dating help worth the money" evaluation; closest sibling in shape and tone
- /blog/profile-pictures — the cheapest variable a matchmaker cannot fix for you; natural "try this first" link
- /blog/tinder-profile-template — the DIY alternative to paying someone to package you
- /blog/why-your-first-tinder-photo-decides-everything — supports the "fix the profile before you spend thousands" argument
- /blog/best-tinder-bios-for-guys — what a matchmaker's copywriter would charge you for, done yourself
- /blog/tinder-openers — matchmakers arrange the date; you still have to carry the conversation

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tawkify` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `tawkify` and stay ≤ 155 characters.
- Use `tawkify` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
