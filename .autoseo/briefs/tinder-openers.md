# Brief: tinder openers

- Target keyword: `tinder openers`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 1100/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Why this topic

The reader has matches and is getting no replies. That is the exact moment
this site's product is relevant, and it is the gap between the site's
profile pages (photos, bios, template) and its later-stage pages (asking
her out). This page should own the sub-cluster: `tinder openers`,
`best tinder openers`, `tinder opening lines`, `how to start a
conversation on tinder` — roughly 3,600/mo combined at near-zero KD.

Keep it clearly distinct from the existing `/blog/pick-up-line` and
`/blog/best-rizz-lines` pages. Those are about canned one-liners. This
page is about the _method_ of a first message that references her actual
profile. It is fine — encouraged, even — to draw that contrast explicitly
and explain why copy-paste lines underperform a specific observation.

## Competitor coverage

- https://textgod.com/tinder-openers/

## Internal-link candidates (up to 6)

- /blog/tinder-profile-template — openers only work once the profile earns the match; the upstream fix for a reader whose opener problem is really a match problem
- /blog/best-tinder-bios-for-guys — a bio that hands her something to react to is what makes her openers land too; pair with the "give her a hook" idea
- /blog/why-your-first-tinder-photo-decides-everything — no match, no opener; useful for the reader who has nobody to message yet
- /blog/ask-girl-out-tinder — the natural next step once an opener turns into a conversation; closes the loop from first message to date
- /blog/does-tinder-have-read-receipts — directly relevant to the reader who sent an opener and heard nothing and is wondering whether she saw it
- /blog/swipe-left-meaning — light context for readers new to the app's basics

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tinder openers` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `tinder openers` and stay ≤ 155 characters.
- Use `tinder openers` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.

Suggested title: `Tinder Openers That Get Replies` (31 chars, keyword first).
