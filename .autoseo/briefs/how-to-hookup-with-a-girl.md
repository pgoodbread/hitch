# Brief: how to hookup with a girl

- Target keyword: `how to hookup with a girl`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 3500/mo · KD: 1
- Target length: 1200–1800 words
- Post type: guide

## Angle

The searcher is already on the apps and already getting some matches — what they
can't do is turn a chat into an actual meetup. Write it as the bridge between
"we matched" and "we're meeting", not as a seduction-tactics listicle.

The competitor page is a tactics dump. The differentiator here is honesty about
what actually moves the needle: your photos decide whether you get the chance at
all, the chat only has to earn a low-stakes meetup, and reading interest
correctly (and backing off when it isn't there) is the skill that separates guys
who get invited back from guys who get unmatched. Consent and directness are the
same thing done well — frame it that way rather than as a disclaimer bolted on
at the end.

## Competitor coverage

- https://roast.dating/blog/how-to-hookup-with-a-girl

## Internal-link candidates (up to 6)

- /blog/profile-pictures — the match has to exist before any of this applies; photos set the ceiling on how many chances you get
- /blog/best-rizz-lines — the opener that earns the reply this whole guide depends on
- /blog/pick-up-line — what to send first when the profile gives you nothing to work with
- /blog/question-to-ask-your-crush — questions that build real interest in the chat instead of stalling on small talk
- /blog/ask-girl-out-tinder — the direct next step: the actual ask, and how to word it
- /blog/negging — the counterpoint; the "tactics" that reliably backfire and why

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `how to hookup with a girl` (exact, or its natural singular/plural), ideally within the first 30 characters. Use the one-word spelling "hookup" so it matches the target phrase.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `how to hookup with a girl` and stay ≤ 155 characters.
- Use `how to hookup with a girl` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
