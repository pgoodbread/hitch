# Brief: tinder bios for guys

- Target keyword: `tinder bios for guys`
- Lane: improve
- Search intent: informational (commercial undertone — the searcher wants copyable examples, then help applying them)
- Funnel position: mid
- Volume: 2500/mo · KD: 7
- Target length: 1200–1800 words
- Post type: guide

## Competitor coverage

- https://textgod.com/flirty-tinder-bios/

## Internal-link candidates (up to 6)

- /blog/why-your-first-tinder-photo-decides-everything — the photo is the gate: the bio only gets read after the first photo earns a tap, so this is the natural upstream link (already linked in the closing section; keep it)
- /blog/dating-me-is-like — a specific bio-prompt format; link it where you cover prompt-style or fill-in-the-blank bios so readers who need one line rather than a whole bio have somewhere to go
- /blog/ask-girl-out-tinder — the downstream step: the bio earns the match, this covers converting it into a date
- /blog/does-tinder-have-read-receipts — link only if you touch what happens after she replies; skip it if the fit is forced

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tinder bios for guys` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- The site's frontmatter schema supports **only** `title` — there is no `seoTitle` field (`src/app/blog/[slug]/page.tsx` reads `post.title` for both the H1 and the <title> tag). Do not add a `seoTitle`; it would be dead frontmatter.
- `description` must contain `tinder bios for guys` and stay ≤ 155 characters.
- Use `tinder bios for guys` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.

## Revise, don't duplicate

- Existing page: /blog/best-tinder-bios-for-guys (Best Tinder Bios for Guys: Examples That Get Matches (and Why They Work)) → file `src/content/blog/best-tinder-bios-for-guys.mdx`
- GSC (90d): position 58.8 (best 56.3), 4 impressions, 0 clicks, top query "tinder bios for guys"
- Task: strengthen the existing page for `tinder bios for guys`. Do not create a new file.
- Title: the current `title` is 72 characters — over the hard 60 maximum, and the page sits at position 56 with 4 impressions, so there is no ranking equity to protect by keeping it. Shorten it to a keyword-first headline ≤ 60 characters (e.g. `Tinder Bios for Guys: Examples That Get Matches`). **Keep the frontmatter/file slug `best-tinder-bios-for-guys` exactly as it is** — the URL and its crawl history must not change.

## Gaps to close against the competitor

The current draft is 1459 words with a solid style-by-style example structure. It is thin in three places the competitor page covers and searchers expect:

- **Example density.** The searcher wants copy-paste lines. Make sure each style section carries several distinct, usable examples rather than one or two — that is the primary reason this query converts on a page.
- **Keyword placement.** `tinder bios for guys` currently appears in the title but not in the opening paragraph or any `##` heading. Work it into the first 100 words and one heading naturally — do not stuff it.
- **Length.** At 1459 words there is headroom to 1800. Spend it on examples and on the short-bio / one-liner case (guys who want a single line, not a paragraph), which the competitor covers and this page does not.

Keep the existing voice, structure, and the `/` AI-profile-review link in the closing section.
