# Brief: tinder algorithm

- Target keyword: `tinder algorithm`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 700/mo · KD: 2
- Target length: 1200–1800 words
- Post type: guide

Why this topic (for the reviewer): the top-scoring candidates in this refresh are
all rival-brand navigational queries (doublelist, secret benefits, tawkify), so the
pick was made on fit rather than raw score. `tinder algorithm` is the closest thing
in the pool to the site's own promise — it explains _why_ profile quality drives
match volume, which is exactly what the optimizer sells — and it anchors a
Tinder-mechanics cluster the site has zero coverage of today (tinder boost,
swipe surge, likes per day, shadowban ≈ 2k/mo of adjacent demand).

## Competitor coverage

- https://roast.dating/blog/tinder-algorithm

## Internal-link candidates (up to 6)

- /blog/tinder-profile-template — the natural next step once the reader understands the algorithm rewards a complete, high-performing profile
- /blog/why-your-first-tinder-photo-decides-everything — the first photo is the single biggest input into swipe-rate, which is what the algorithm actually measures
- /blog/best-tinder-bios-for-guys — bio quality feeds the same engagement signals the algorithm ranks on
- /blog/is-tinder-gold-worth-it — readers asking about the algorithm almost always also ask whether paid tiers buy them reach
- /blog/swipe-left-meaning — basics context for readers who arrive without the vocabulary
- /blog/tinder-openers — match volume is wasted without replies; the obvious follow-on read

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tinder algorithm` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `tinder algorithm` and stay ≤ 155 characters.
- Use `tinder algorithm` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.

## Angle notes

- Be honest about what is known vs. speculated: Tinder retired the public Elo-score
  framing, so avoid stating Elo as current fact — cover what Tinder has actually
  said and what the observable behaviour implies.
- Keep it actionable: every mechanic explained should end in something the reader
  can change about their profile or swiping behaviour.
- Avoid growth-hack myths (swipe-reset "tricks", mass-liking) — the site's voice is
  the honest-answer one used in `Is Tinder Gold Worth It?`.
