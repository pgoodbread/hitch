# Brief: rizz app

- Target keyword: `rizz app`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: commercial
- Funnel position: bottom
- Volume: 4200/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Competitor coverage

- https://web.rizz.app/

## Internal-link candidates (up to 6)

- /blog/rizz — defines the term a reader searching "rizz app" may still be fuzzy on; natural first-section link
- /blog/best-rizz-lines — the manual alternative to an app; pairs with any "do you even need an app" section
- /blog/pick-up-line — same job, opener-level; good for the "what these apps actually generate" section
- /blog/best-tinder-bios-for-guys — apps get judged on bio output, so link where bio quality comes up
- /blog/profile-pictures — the honest counterpoint: no app fixes a weak photo lineup
- /blog/ask-girl-out-tinder — the step after the opener lands, for the "what happens next" close

## Angle

The reader is shopping: they've seen a rizz app ad and want to know whether one is worth
installing and which does what. Give a straight answer built on what these apps actually
do (screenshot-to-reply generators, bio writers, opener generators), where they help, and
where they reliably fail (a generated line on a weak profile still gets no match). Be
concrete about the categories rather than ranking brands. Do not oversell — the honest
verdict is the ranking asset here.

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `rizz app` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `rizz app` and stay ≤ 155 characters.
- Use `rizz app` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
