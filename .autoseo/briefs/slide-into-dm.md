# Brief: slide into dm

- Target keyword: `slide into dm`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: top
- Volume: 3300/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Competitor coverage

- https://textgod.com/how-to-slide-into-dms/

## Internal-link candidates (up to 6)

- /blog/pick-up-line — the opener itself; a DM is only as good as the first line in it
- /blog/best-rizz-lines — concrete line examples readers can lift straight into a DM
- /blog/profile-pictures — the DM gets judged against your profile the second they tap your name
- /blog/ask-girl-out-tinder — the natural next step once the DM turns into a conversation
- /blog/question-to-ask-your-crush — how to keep the thread alive after they reply
- /blog/good-morning-text-for-him — follow-up cadence once you're actually texting

## Angle notes

- The phrase is slang; readers arriving here range from "what does this mean" to "I have someone specific in mind". Answer the meaning fast, then spend the article on execution.
- Cover the platform split: Instagram DMs, Tinder matches, and replying to a story are different openers with different expectations.
- Be honest about the failure mode — generic "hey" DMs, opening cold with a compliment on looks, double-texting before a reply. That honesty is the site's voice.
- Tie back to the profile: a DM lands or dies based on what they see when they tap your name.

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `slide into dm` (exact, or its natural singular/plural — "slide into DMs" is fine), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- If the best human-readable headline cannot carry the keyword, keep it as `title` and add a keyword-first `seoTitle` (≤ 60 chars) — the site uses `seoTitle` for the <title> tag and keeps `title` as the on-page H1.
- `description` must contain `slide into dm` and stay ≤ 155 characters.
- Use `slide into dm` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
