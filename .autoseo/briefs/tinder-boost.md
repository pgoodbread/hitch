# Brief: tinder boost

- Target keyword: `tinder boost`
- Lane: new
- Destination: src/content/blog (renders under /blog)
- Search intent: informational
- Funnel position: mid
- Volume: 800/mo · KD: 0
- Target length: 1200–1800 words
- Post type: guide

## Why this topic

The site's 22 pages are almost entirely bios, photos and texting. The Tinder
_mechanics_ cluster is uncovered, has no cannibalisation risk, and sits closest
to the product's promise. `tinder boost` is the strongest term in that vein at
KD 0. The searcher is already on Tinder and deciding whether to spend on a
paid feature — the same decision the site already answers for Gold, so the
angle is the honest "what it does, when it pays, what it can't fix" treatment,
landing on the fact that Boost multiplies your existing conversion rate.

## Competitor coverage

- https://roast.dating/blog/tinder-boost
- https://textgod.com/tinder-boost/

## Internal-link candidates (up to 6)

- /blog/is-tinder-gold-worth-it — Boost is bundled into Gold; the site's other honest "is this paid feature worth it" evaluation and the closest sibling in shape and tone
- /blog/why-your-first-tinder-photo-decides-everything — Boost buys impressions, the first photo decides what those impressions convert at; the core multiplier argument
- /blog/profile-pictures — the free fix to make before paying for reach
- /blog/tinder-profile-template — the structural profile fix that raises the rate a Boost multiplies
- /blog/tinder-openers — Boost gets the match; the opener is what decides whether it goes anywhere
- /blog/swipe-left-meaning — sibling Tinder-mechanics explainer for readers still learning how the queue works

## Title & metadata (the site renders frontmatter `title` as the H1 and as the <title> tag)

- `title` must contain `tinder boost` (exact, or its natural singular/plural), ideally within the first 30 characters.
- Keep `title` ≤ 44 characters so the site can append its brand suffix; hard maximum 60.
- The site's content schema (`src/lib/content.ts`) has no `seoTitle` field — do not add one; the `title` must carry the keyword.
- `description` must contain `tinder boost` and stay ≤ 155 characters.
- Use `tinder boost` in the first 100 words and in at least one `##` heading.
- Never write a `# ` heading in the body — the page would get two H1s.
